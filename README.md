# assignment2
task-1: the code provides an if else code where if a number is divided by 2 and gives remainder zero
        then it is even else odd number.
task-2: a for loop simple syntax where a variable sum is taken initially zero then is added to loop variable at every iteration and the sum value is updated at every iteration
        hence after every iteration the sum increases and becomed 1225 after 50 iteration then printed the sum.
